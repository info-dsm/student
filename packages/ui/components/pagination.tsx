import * as React from "react";
import styled from "styled-components";
export interface PaginationProps {
  nowIndex: number;
  last: number;
}
export const Pagination = ({ nowIndex, last }: PaginationProps) => {
  return (
    <>
      <div></div>
    </>
  );
};
