import { ReactNode } from "react";
export interface TableProps {
  children: ReactNode;
}
export const Table = ({ children }: TableProps) => {
  return (
    <>
      <div>안녕하세여</div>
    </>
  );
};
